# Development State

## Current Position
- **Active Phase:** Phase 1 (Native Solana Program Scaffold)
- **Status**: Unstarted. Getting ready to scaffold the raw solana BPF `pocketchange_vault` project.

## Blockers & Open Questions
- Need to check if `cargo build-sbf` is installed properly, if not we will build via standard rust or adapt.
- Using a raw Solana Rust program without Anchor to maintain build speed and compatibility. 
