# Trading Changelog

## Unreleased

### Changed
- 2026-04-22: Deployed the complete Unified Mathematical Trading Framework (4 layers) into the live Tokyo sniper: weighted signal fusion (Layer 1), fractional Kelly sizing (Layer 2), volatility regime detection (Layer 3), and Bayesian hyperparameter tuning (Layer 4). The pipeline is fully wired and actively computing expected values on every live candidate.
- 2026-04-22: Fixed the velocity signal strength calculation in the signal fusion layer that was dividing by 100 and causing near-zero probabilities; Kelly expected values are now realistic (`p=27-35%`).
- 2026-04-22: Replaced the undefined `priceChg5m` variable with `momentum5m` in the synthetic ATR volatility computation.
- 2026-04-22: Added boot-time seeding of the `_kellyTradeHistory` buffer from the existing `trade_history.jsonl` archive so the Kelly sizing module can compute a real historical win/loss ratio (`b=0.79`) immediately instead of starting from a naive `1.0` default.
- 2026-04-22: Transitioned Kelly sizing from log-only mode to live active sizing with a conservative 0.01 SOL maximum cap. When Kelly determines no mathematical edge exists, the system safely falls back to fixed position sizing.
- 2026-04-22: Reopened Tokyo’s pump.fun bonding-curve route path where every post-cooldown candidate was regenerating `ZERO_LIQ` cooldowns despite a live Jupiter quote. Routable pump launchpad names now bypass the zero-liquidity hold/block gate, switch into `pump-direct` quoting, and continue through the existing low-liquidity execution path instead of being auto-cooled for 30-120 seconds.
- 2026-04-22: Added a new `pcp-catalyst` polling service that scans DexScreener boosts, token profiles, and paid orders every 20 seconds, diffs profile snapshots for added or removed socials, and folds GMGN CTO hints into durable `signals/catalyst_alerts.json` / `signals/profile_snapshots.json` artifacts for the live stack.
- 2026-04-22: Wired catalyst alerts and wallet alpha consensus into the live qualifier path through a new `alpha_signal_provider.ts`. Tokyo’s sniper now logs `ALPHA_BOOST`, carries catalyst context into `FINAL GATE READY`, and uses boosted confidence instead of leaving fresh alpha catalysts invisible at entry evaluation time.
- 2026-04-21: Reopened a bounded route-live zero-liquidity scalp path for Tokyo’s micro lane. When rolling terrain already shows strong 5-minute price response, low off-peak drift, and healthy flow, the sniper can now pass that zero-liq route-live candidate before route strength formally recovers, and stale zero-liq route cooldowns are shorter so underfilled-book rechecks happen in seconds instead of minutes.
- 2026-04-21: Stopped Tokyo’s live velocity lane from hard-skipping every fresh strong-flow name with a mildly negative `1h` print. Real non-synthetic candidates can now pass a bounded `NEG 1H RECOVERY` override when they are still fresh, current flow is clean, and `5m` response is already positive, instead of being discarded by the blunt trending cross-check.
- 2026-04-21: Opened Tokyo’s micro-scout lane back up in two targeted ways after live observation showed qualified route-live candidates still dying before execution. Weak-route probes with real near-peak price response can now pass a bounded early-price-response quality window instead of being hard-blocked outright, RugCheck soft warnings now feed into position sizing instead of disappearing before `ENTRY RISK`, jitter-bundle scoring is less punitive, and stale Redis position locks now self-clear when the wallet no longer holds the token.
- 2026-04-21: Loosened Tokyo’s velocity recovery tiers just enough to stop shallow fallback starvation. `tier2` now starts at `5 buys` instead of `6`, and `tier3` is now a genuinely looser fallback (`3 buys`, `0.60` buy ratio, `0.4 SOL` flow, deeper shortlist) instead of paradoxically requiring a stronger buy ratio than `tier2`.
- 2026-04-21: Forced Tokyo deploy validation into single-concurrency test mode after the remote Node test runner hit a parallel V8 fatal despite green local results. Live sniper rollouts are still gated by the full suite, but now by a deterministic remote run instead of a harness-level crash.
- 2026-04-21: Capped refinement-only synthetic names inside each velocity poll so composite GMGN/Bags/trending candidates cannot crowd out the whole live shortlist. Tokyo still prioritizes real non-synthetic names first, but now only a bounded number of synthetic/refinement candidates can consume a single pass before the loop moves on.
- 2026-04-21: Stopped weak-momentum soft-recheck churn from monopolizing Tokyo’s velocity loop. `WEAK_MOMENTUM` is no longer a soft-cooldown reuse reason, and flat `gmgn-bridge` names that still lack a real `1m` confirmation now get a tested `90s` cooldown instead of being recycled every few seconds as “still-strong” leaders.
- 2026-04-21: Tightened Tokyo’s synthetic/composite velocity lane so flat synthetic GMGN names stop monopolizing the live shortlist. The sniper now holds synthetic candidates earlier when rolling terrain still shows effectively flat 5-minute response despite live market evidence, prioritizes non-synthetic/non-refinement candidates before synthetic ones during velocity assessment, and applies a longer `SHADOW_MOM_HOLD` cooldown to flat synthetic names that still reach the shadow lane.
- 2026-04-21: Fixed the remaining paper/live journal contamination path and stale-paper-runtime drift. `wallet_recovery.ts` now uses the same ghost-signature and paper-isolation journaling rules as the hardened sniper, the Tokyo deploy helper now ships the newer family/risk helper modules plus the recovery script, and live reloads now refresh `pcp-sniper-paper` as well so the paper lane cannot keep leaking stale `PAPER_TRADE_*` rows into the live journals after sniper updates.
- 2026-04-21: Disabled Tokyo’s `matureFallback` lane after verifying the last 20 live exits were all mature-fallback micro probes and netted `3 wins / 17 losses / -0.001362105 SOL`. Profit-first routing now keeps the lane off until it can be revalidated with a controlled sample instead of letting stale fallback names keep grinding down the wallet.
- 2026-04-21: Finished the DMind switchover and isolated paper-mode journal writes from live trade history. Tokyo’s deploy helper now forces `GEMMA4_MODEL` / `OLLAMA_MODEL` to `dmind-risk`, reloads the refiner plus the DMind-reviewed Wiggum loops, and the sniper no longer lets `PAPER_TRADE_*` rows contaminate the live `trade_journal.jsonl` / archive paths.
- 2026-04-21: Fixed the live sniper’s mature-fallback lane so it stops chasing older, already-expanded GMGN names while fresh velocity still has real candidates to refine. Mature fallback now enforces explicit age and overextension ceilings, no longer relaxes buy-ratio or buy-count thresholds, defers entirely when fresh velocity still has eligible names, and uses capped normalized momentum in shortlist scoring instead of multiplying by raw `5m%`, which had been letting blown-out names dominate the rotating fallback pool.
- 2026-04-19: Restored Tokyo to the intended Helius primary RPC/WSS with Chainstack kept only as backup, then tightened the synthetic execution handoff that was still clogging the live loop. Synthetic/composite velocity rows may still build rolling terrain and pass refinement, but they now must earn an independent live pair or live Jupiter route confirmation before they can graduate into `VELOCITY ENTRY` or any micro-only downshift. This removes the last cached-GMGN shortcut that was letting names like `PIGEON`, `DA`, and `SOLANALIFE` keep reappearing as tradable despite only synthetic market evidence.
- 2026-04-19: Stopped synthetic/composite fallback values from acting like live orderflow in Tokyo’s sniper loop. Composite velocity entries are now explicitly `refinementOnly`, their synthetic estimates are capped conservatively, and the live sniper requires rolling terrain confirmation plus real market structure before any synthetic-seeded candidate can execute. Placeholder launchpad rows still hard-block immediately, but the broader composite GMGN/Bags/wallet fallback path now feeds terrain refinement instead of direct entry.
- 2026-04-19: Fixed Tokyo drift recovery all the way through execution. The live deploy helper now uploads the missing signal/strategy helper files that had left Tokyo on stale sniper logic, the Chainstack cutover helper now keeps a backup `RPC_ENDPOINT_2` on the public Solana endpoint, and the live sniper now treats provider-capacity failures as transport failovers rather than bad trades by retrying signed simulation/send confirmation on the backup RPC when Chainstack returns quota or rate-limit errors.
- 2026-04-18: Cut Tokyo’s read-heavy Solana paths over to Chainstack HTTPS/WSS using the path-authenticated endpoints, and disabled the burned Yellowstone geyser settings so the velocity stream stops wasting reconnect budget on dead credits. Verified from Tokyo that `{{CHAINSTACK_WSS_URL}}` accepts Solana subscriptions, `velocity.json` repopulates immediately after restart, and the live feed can stay populated from Chainstack instead of starving on the old Helius saturation.
- 2026-04-18: Fixed the new Tokyo target drought caused by Jupiter quote rate limiting. The sniper now keeps an explicit quote backoff window when Jupiter returns `429`, skips duplicate fallback quote attempts during that window, and applies short per-mint `JUP_RATE_LIMIT` cooldowns to route-live zero-liquidity probes instead of hammering the same expensive quote path every poll.
- 2026-04-18: Added a bounded terrain-assisted continuation proxy for missing `1m` candles in Tokyo’s velocity lane. When the short candle is genuinely absent, repeated clean flow can now satisfy continuation after multiple healthy terrain samples, but only for high-ratio names that are still near their local peak. This opens some of the repeated `8-9 buy / 100% ratio` missing-`1m` bursts without reopening the weaker `73%` junk bucket.
- 2026-04-18: Expanded Tokyo’s empty-book velocity assessment so it can supplement primary leaders with deeper recovery tiers in the same poll instead of only falling back when the primary lane has zero eligibles. When the book is underfilled, the sniper now keeps pulling tiered fresh candidates until it reaches a bounded working set for that poll, which helps it assess several simultaneous spikes instead of over-focusing on the first few leaders.
- 2026-04-18: Added a bounded underfilled-book scout profile so Tokyo stops waiting for perfect no-DEX bursts while the 10-slot book is mostly empty. When the book is at or below 30% filled, the micro-scout lane now temporarily relaxes its raw no-DEX floor to `8 buys / 72% ratio / 1 SOL / 8 tx-min` and widens scout throughput to `8` candidates per poll, then automatically returns to the stricter baseline once positions start filling.
- 2026-04-18: Switched Tokyo portfolio sizing from full-send deployment to slot-aware deployment and raised the live cap to 10 concurrent positions. Micro-only portfolio buys now consume one remaining-slot share of deployable SOL instead of the whole deployable treasury, while PM2 and the sniper guard both allow up to 10 open positions so capital can stay diversified across the book.
- 2026-04-18: Broadened the weak-momentum shadow lane without loosening live execution. Tokyo now treats bounded `5 buys / 1 SOL / 5 tx-min` near-miss bursts as shadow-assessment candidates for one short recheck, while keeping the stricter `8 / 1.5 / 8` floor for buy-ratio holds. This widens coverage for names like the fresh `5B/0S` velocity bursts that were being hard-skipped before a second sample, but it still stays assessment-only and leaves the live trade gate untouched.
- 2026-04-18: Added a bounded shadow-assessment lane for near-miss strong-flow entries. Tokyo can now give `buy_ratio_below_threshold` and `weak_momentum_skip` names one short recheck window when they are close to the floor and terrain still looks constructive, instead of hard-dropping them on first contact. The lane is intentionally selective, telemetry-rich, and bounded by short cooldowns plus terrain span limits so it widens assessment without reopening obvious junk.
- 2026-04-18: Fixed the remaining micro-scout sizing handoff gap. Several direct Tokyo micro-scout entry paths were still constructing fixed `0.001 SOL` options by hand, which bypassed the new portfolio-sized treasury deployment logic even though the config was live. Those paths now share the same micro-scout option builder as the downshifted normal lane, and the live logs now describe portfolio-based sizing instead of implying every probe is fixed-size.
- 2026-04-18: Replaced fixed micro-only probe sizing with portfolio-based treasury deployment. Tokyo micro-scout entries can now size as a configured fraction of deployable native SOL while preserving a hard reserve and fee buffer, so the wallet can keep trading in perpetuity instead of stalling at a tiny fixed buy size.
- 2026-04-18: Tightened the remaining late-entry leak in the micro-scout lane. Tokyo now holds or blocks probe-like buys when terrain shows price has already rolled off the recent peak, unless route strength and repeated strong-flow samples are good enough to justify a genuine late recovery pass.
- 2026-04-18: Added a very narrow route-live zero-liquidity fast-track for profit-first scalp entries. Tokyo can now enter the first route-live sample only when current flow is exceptional and the live 5-minute move is inside a validated scalp window, and those buys are tagged as `MICRO_SCOUT_FAST_TRACK` / `routeLiveFastTrack` so we can measure whether earlier timing improves expectancy.
- 2026-04-18: Refined the micro-scout terrain-quality gate with two narrow exceptions from Tokyo replay data. Very early weak-route probes with promising momentum now take one short confirmation hold instead of an immediate hard block, and only deeply sampled high-momentum runners with mid-quality route strength can pass above the normal momentum cap. The recent replay improved from `5` allowed trades / `+0.000453794 SOL` to `6` allowed trades / `+0.000748458 SOL` without reopening the broad weak-route trash bucket.
- 2026-04-18: Added a micro-scout terrain-quality gate so Tokyo no longer buys every loud route-live probe. Micro probes now need either real route-strength confirmation or a validated 5-minute momentum window; otherwise they hold for more samples or block early.
- Added a narrow 1-hour buy-count override for fresh strong-flow names. Tokyo no longer forces probe-like entries to wait for stale `buys1h` history when the live tape is already showing `8+ buys`, strong ratio, and real SOL flow, which was causing some fresh movers to be reconsidered only after their initial traffic burst had already passed.
- Added a terrain-level flow-decay guard for late entries. Tokyo now compares current short-horizon flow against each mint’s own recent peak and blocks or holds probe-like buys when traffic has already faded and the live 5-minute tape is materially off its local high, while still allowing consolidations that retain exceptional route strength or real liquidity growth.
- Stopped recovery tiers from starving on the first stale names. Tokyo now over-selects each velocity recovery tier before cooldown filtering, so `VELOCITY-FIRST` can scan past the first 8 cooling-down leaders and still find later eligible names in the same fallback tier.
- Relaxed the velocity continuation fallback just enough to stop rejecting extreme live-flow names on a one-point buy-ratio miss. Tokyo now treats `12+ buys`, `80%+ buy ratio`, `14+ tx/min`, and `6+ SOL/60s` as valid missing-`1m` continuation in velocity mode, so names like the recent `13B/3S` burst stop dying at `WEAK MOMENTUM SKIP` purely because they printed `81%` instead of `82%`.
- Tightened the route-live zero-liquidity allow path so Tokyo no longer treats a single hot 5-minute price print as enough proof for a zero-liq micro probe. Structural recovery through route-strength or liquidity growth can still pass immediately, but price-only recovery now needs repeated terrain samples plus a stronger live 5-minute move before entry is allowed.
- Tightened the zero-liquidity route-live probe gate against fake movement. Tokyo now hard-blocks route-live zero-liquidity entries that are still materially negative on the 1-hour trend and flat on the live 5-minute tape unless route strength or liquidity are actually recovering, so “traffic without travel” names stop slipping through as micro-scouts.
- Added a tiered velocity-recovery shortlist so `VELOCITY-FIRST` can keep scanning fresher lower-intensity names when the strict accelerating leaders are all cooling down or temp-blacklisted. Tokyo still uses the original high-conviction tier first, but if that pool yields zero eligible names after cooldown filtering it now falls back to tighter buy-ratio rescue tiers instead of spending whole polls on `0/x eligible`.
- Tightened the zero-liquidity route-live path so Tokyo stops probing the same non-improving ghost pairs as if they were fresh launches. Strong-flow zero-liq names still get one short confirmation hold, but once terrain shows repeated samples with no liquidity growth, no route-strength improvement, and no price response, the live sniper now escalates cooldowns and blocks route-live entry instead of continuing to micro-buy empty terrain.
- Reduced repeated normal/velocity churn on the same mint after buy-pressure failures. The live sniper now gives standard-lane `buy_ratio_below_threshold` and `buys_below_threshold` rejects short configurable cooldowns, while the velocity shortlist still allows near-expiry strong-flow rechecks for those reasons so hot names can recycle back in once they have actually changed.
- Reduced `VELOCITY-FIRST` starvation when hot leaders are only under short-lived soft cooldowns. The live sniper now distinguishes soft revisit reasons like `MICRO_CONTINUATION`, `TERRAIN_PRECHECK`, `ZERO_LIQ`, and `WEAK_MOMENTUM` from hard rejects, allowing a tiny number of near-expiry strong-flow names back into the shortlist instead of letting cooling leaders consume the entire velocity lane.
- Made the earliest route-live `LIVE_DUMP` and `OVERBOUGHT` filters terrain-aware instead of one-shot kills. Moderate low-liquidity route-live anomalies now take a short terrain confirmation hold on the first sample and can pass on later samples when route strength, price response, or liquidity are improving, while truly extreme dumps and blowoff tops still hard-reject immediately.
- Tightened the new terrain-memory loop so it can influence live micro probes instead of just recording them. Probe-like downshifts now qualify for the first route-live confirmation hold, route-live 8-buy bursts count as strong-flow terrain samples, and the confirmation retry was shortened so hot Tokyo micro-scouts can gather a second sample before the move disappears.
- Fixed a hidden valuation blind spot in the live sniper. DexScreener pair normalization now carries `marketCap`, `fdv`, and the extra volume windows the runtime was already trying to read, and the entry path now applies a configurable FDV-to-liquidity guard with lane-specific thresholds plus telemetry on rejected and accepted trades.
- Narrowed the mature-trending fallback so it stops burning scan budget on the same stale GMGN names. The live sniper now requires a stronger mature-candidate buy-ratio floor before those older tokens even enter the rotating shortlist, applies lane-specific cooldowns when mature candidates fail on buy-ratio or 1-hour buy-count thresholds, and reads those settings from a new `matureFallback` strategy-profile block instead of hardcoding the churn behavior.
- Added a scored bundler/simulated-traffic guard to the live sniper. It now scores strong flow with flat price response, high turnover versus liquidity, low-holder fresh churn, and holder-shape jitter before entry; only the strongest low-liquidity cases are blocked, while warnings and scores are persisted into buy telemetry and missed-target stats for later tuning.
- Synced the local active Tokyo strategy profile back to the live VPS shape and lowered `microScout.reserveSol` from the hotfixed `0.8000` choke point to `0.7900`, so `liveTest.microOnly` can actually place `0.0010 SOL` probes again when treasury is hovering around `0.8009 SOL` instead of self-blocking every candidate on `deployable treasury 0.0009 below minimum 0.0010`.
- Fixed learning skew for micro-only probe entries. Downshifted normal-lane probes now resolve to `entryMode: "micro-scout"` before they hit `trySnipe()`, so BUY/SELL journal rows, open-position state, and profile-learning buckets stop treating fixed-size `liveTest.microOnly` probes as full normal entries.
- Fixed the remaining profitable-wallet bypass in micro-only mode. Wallet-led `HIGH_CONV` entries now route through the same shared probe-option helper as the normal-lane downshift paths, so wallet-triggered probe trades inherit fixed micro sizing and journal as `entryMode: "micro-scout"` instead of silently defaulting to `normal`.
- Added a live Redis position-lock guard at the top of `trySnipe()` so Tokyo cannot re-enter the same mint just because in-memory position state drifted or the process restarted while the live position lock was still active.
- Stopped treating zero-liquidity DexScreener pairs as executable live DEX listings and capped the normal lane’s apex-overlay market-cap bypass below the structural-exit zone, so giant-cap thin-air names and `liq $0` ghosts stop slipping into the active sniper path.
- Removed another velocity-lane self-contradiction by letting continuation-approved velocity entries bypass the duplicated normal-lane 5-minute momentum floor inside `trySnipe()`. The velocity path now still pays later liquidity, market-cap, and route checks, but it no longer green-lights a mint in `VELOCITY-FIRST` and then immediately self-rejects it on the same short-momentum rule one function later.
- Narrowed the next continuation-path contradiction in the live normal lane. `scripts/maintain/volume_override_logic.ts` now allows the low-volume override for continuation-approved velocity names only when they are fresh, near the normal 1-hour volume floor, already have real liquidity, and show either positive `1m` momentum or materially stronger live flow; `scripts/maintain/momentum_sniper.ts` passes that continuation context through so the bot stops rescuing a flat `5m` mint in `VELOCITY-FIRST` only to kill it instantly on the standard volume floor one step later.
- Rebalanced the live normal-lane profile back toward the actual winning micro-cap buckets. The active strategy now uses a `$50k` market-cap floor, `$25k` 1-hour volume floor, and `$15k` live-liquidity floor instead of the temporary `$100k/$50k/$50k` choke profile, while keeping the stricter `10%` 5-minute momentum floor and the `1m` market-cap ceiling intact.
- Relaxed the velocity-mode continuation fallback just enough to stop starving clean early bursts while `1m` candles are still missing. `scripts/maintain/continuation_signal_logic.ts` now treats `9 buys / 100% buy-ratio / 3.5+ SOL / 8+ tx-min` as valid velocity-mode continuation, and the matching test locks that narrower rescue case into `npm test`.
- Verified that the current live drought was still happening before `executeSwap()`, then narrowed the next late gate. `scripts/maintain/entry_qualifier_logic.ts` now gives clean continuation-approved velocity entries a slightly lower qualifier threshold (`42%` instead of `45%`) only when their `60s` flow is strong enough to justify it, and `scripts/maintain/momentum_sniper.ts` uses that threshold so real pre-execution candidates stop dying on an arbitrary few-basis-point miss.
- Verified the current live drought is still pre-execution rather than a broken chain-send path, then replaced the blunt zero-liquidity cooldown with a flow-aware recheck plan. `scripts/maintain/zero_liquidity_logic.ts` now shortens indexed `$0`-liquidity retries to `10-20s` when the live `60s` flow is strong enough to justify it, and `scripts/maintain/momentum_sniper.ts` carries that plan into both the velocity-first lane and `trySnipe()` so the bot can catch executable liquidity when it actually appears instead of sleeping through the launch.
- Taught the normal 1-hour volume floor to distinguish between dead low-volume names and fresh high-flow names that already have real liquidity. `scripts/maintain/volume_override_logic.ts` now provides a tested override for new velocity candidates with strong live orderflow, positive short-term momentum, and at least `$5k` of pool liquidity, and `scripts/maintain/momentum_sniper.ts` uses that override so the swarm can keep evaluating legitimately fresh names without reopening the zero-liquidity junk lane.
- Reduced another self-inflicted velocity-lane choke point by replacing the brittle inline qualifier score with a tested helper. `scripts/maintain/entry_confidence_logic.ts` now combines TA confidence, capped structural buy-ratio confidence, participation/volume bonuses, and a small strong-live-flow bonus, and `scripts/maintain/momentum_sniper.ts` uses that helper instead of letting a weak TA score dominate otherwise healthy velocity bursts. `scripts/maintain/entry_confidence_logic.test.js` locks the new blend into `npm test`.
- Reduced velocity-lane false negatives on fresh, flat-candle names. `scripts/maintain/momentum_sniper.ts` now treats negative 5-minute structure and flat 5-minute structure differently in `VELOCITY-FIRST`: genuinely negative `5m` names still cool down immediately, but borderline `0% < 1%` candidates can continue when `scripts/maintain/continuation_signal_logic.ts` confirms missing-`1m` continuation from strong live orderflow in `velocity` mode. This preserves dump protection while letting real live-flow bursts reach the deeper sniper checks.
- Smoothed the live profit loop by making swap success depend on real chain confirmation instead of mere submission. `scripts/maintain/momentum_sniper.ts` now derives the serialized Jupiter transaction signature, waits for `confirmed` status (using `confirmTransaction` when blockhash metadata is available and falling back to signature-status polling), and only returns a successful BUY/SELL signature to the rest of the loop after that confirmation lands. This closes a long-standing bookkeeping gap where positions and realized PnL could advance before the chain had actually accepted the swap.
- Tightened the live normal lane to the only buckets that were positive in the 24h replay and made the swarm’s trade stream more uniform. `scripts/maintain/momentum_sniper.ts` now enforces a `normalLane.minMarketCapUsd` floor alongside the stronger 5-minute momentum, 1-hour volume, and live-liquidity gates, and it mirrors real BUY/ALPHA_SELL rows into Redis only when the signature passes the same ghost/orphan safeguards as the file journal. The same pass also teaches `VELOCITY-FIRST` to filter out cooled-down leaders before choosing candidates, so Tokyo logs now show how many accelerating mints are actually eligible instead of pretending every top-ranked spike is still tradable.
- Tuned the live normal lane toward the only 24h buckets that were actually positive. `scripts/maintain/momentum_sniper.ts` now applies a strategy-profile `normalLane` guard with a stricter 5-minute momentum floor, a 1-hour volume floor, a stronger live liquidity floor, and a market-cap ceiling that rejects mature `1m+` names unless the apex dossier is unusually strong. The same pass also teaches orphan recovery to remember ghost/no-route mints in Redis so restart-time dead sells stop repeating on every boot.
- Reduced live re-evaluation churn in the active PM2 sniper path. `scripts/maintain/momentum_sniper.ts` now preserves the real missed-target writer instead of shadowing it with a no-op later in the file, stamps short Redis cooldowns on repeated `WEAK MOMENTUM SKIP` / `CRASHING SKIP` velocity rejects, and skips wallet-signal `HIGH_CONV` logging altogether when the mint is already cooling down. That keeps PM2 logs cleaner and ensures the reject-learning files actually reflect the current live filter behavior.
- Hardened the live journal and active PM2 sniper against ghost executions. `scripts/maintain/momentum_sniper.ts` now blocks impossible live signatures before opening positions or journaling exits, `scripts/maintain/trade_journal_logic.ts` rejects both `PAPER_TRADE_...` and all-ones signature sentinels for live BUY/SELL rows, and orphan/alpha exit paths inherit the same safeguard so the learning set stops being polluted by fake fills.
- Fixed reject telemetry visibility by teaching both `scripts/maintain/overview_data.ts` and `temp_ssh/pm2_summary.js` to read the real `signals/missed_target_stats.json` shape (`byReason`) instead of the stale `reasons` key. The live overview and Tokyo PM2 helper can now surface the dominant reject buckets again instead of looking falsely empty while the sniper is actively filtering names.
- Tightened the wallet-signal lane so single-wallet “HIGH_CONV” mints without Bags or market support no longer monopolize the poll loop. Unsupported one-wallet names now get a short cooldown instead of being re-evaluated every few seconds, and wallet-sourced buy-pressure rejects also stamp a brief cooldown to preserve scan budget for fresher candidates.
- Lowered live RPC pressure in the PM2 swarm config by slowing `pcp-wallet-monitor` batches and `pcp-bags-swarm` queue drain timing, then updated `temp_ssh/deploy_live_gap_fixes.js` to ship those config/test/helper changes and reload the affected apps through the ecosystem file so Tokyo actually picks up the calmer runtime knobs.
- Synced several previously remote-only safeguards into the actual live PM2 entrypoint `scripts/maintain/momentum_sniper.ts`: missed-target logging now updates `signals/missed_target_stats.json` again instead of acting as a near-no-op, normal entries now reject preflight setups that already satisfy the apex thin-air-liquidity exit heuristic, time-based exits no longer cut winning positions just because the max-hold timer elapsed, and partial take-profit sells now retain the remaining bag with updated size/cost basis instead of orphaning the remainder while journaling a full exit.
- Extended the active sniper’s reject telemetry into the hot-path buy-pressure gates so common live rejects such as weak buy ratio, insufficient buys, and volume/liquidity divergence now populate `signals/missed_target_stats.json` instead of disappearing as bare PM2 log lines.
- Smoothed the live wallet-intelligence lane in `scripts/maintain/wallet_monitor.ts` so Tokyo stops hammering the RPC on every cycle. The monitor now ingests `signals/trending.json` through the same mixed-shape normalizer as the sniper, polls tracked wallets in round-robin batches with an inter-wallet delay, keeps prior state when individual wallets hit 429 rate limits, and stamps `generated_at` plus batch metadata into `signals/wallet_signals.json` so the rest of the swarm can reason about freshness.
- Added `pcp-bags-swarm` to the overview data service focus set so the operator page reflects the new Bags cross-launchpad producer alongside the rest of the live swarm.
- Built the missing Bags producer as `scripts/maintain/bags_swarm_discovery.ts` and added it to PM2 as `pcp-bags-swarm`. The worker watches multiple launchpad programs (Raydium V4, Pump.fun, Moonshot, Bags.fm, Meteora), resolves candidate mints from confirmed transactions, confirms there is an actual `bags-fm` pair on DexScreener, injects a normalized Bags entry into `signals/trending.json`, records recent detections in `signals/bags_swarm.json`, and publishes a conservative velocity hint on Redis so the live sniper can react immediately. `scripts/maintain/bags_swarm_logic.ts` and `scripts/maintain/bags_swarm_logic.test.js` cover mint extraction, trending-entry shaping, and velocity estimation, and `temp_ssh/deploy_bags_swarm.js` deploys the new worker plus the sniper-side Bags-consumption fix together.
- Fixed a live signal-ingestion mismatch that was preventing GMGN and Bags-style trending entries from helping the sniper. `scripts/maintain/momentum_sniper.ts` was still reading `signals/trending.json` as `{ mints: [...] }`, while the bridge writes a flat Dex-style array. A new `scripts/maintain/trending_signal_logic.ts` normalizer now supports both shapes, extracts mint/symbol/volume/liquidity/orderflow consistently, and flags `bags-fm` / `bags-swarm` entries so the wallet-signal lane, velocity-first loop, and Last Stand scan can all consume those signals instead of silently treating the file as empty. `scripts/maintain/trending_signal_logic.test.js` locks the mixed-shape parsing into `npm test`.
- Added a lightweight operator overview service for the live test window. `scripts/maintain/overview_server.ts` serves a single status page backed by `scripts/maintain/overview_data.ts`, showing swarm health, session realized PnL, open unrealized PnL, open positions, recent trades, wallet-intel counts, top reject reasons, velocity hydration misses, and learning-bucket summaries from the existing signal files. `scripts/maintain/overview_data.test.js` covers the summary math, `package.json` adds an `overview` script, and `ecosystem.config.js` now includes `pcp-overview` so Tokyo can run the page under PM2 without another ad hoc watcher.
- Moved the per-mint Redis cooldown check up into the live velocity-first loop in `scripts/maintain/momentum_sniper.ts` so Tokyo stops logging `VELOCITY ENTRY` and spending live Dex checks on mints that are already cooling down post-trade. This closes a self-inflicted scan-budget drain that was starving fresher candidates while the real cooldown rejection still happened later inside `trySnipe()`.
- Added loop-health safeguards to the live sniper runtime: `scripts/maintain/momentum_sniper.ts` now records per-mint velocity hydration misses instead of the misleading blanket `velocity.json not available` message, deduplicates trade-journal destinations so live rows are not written twice to the same file, and skips journaling stale orphan-recovery sells that do not correspond to tracked positions. `scripts/maintain/trade_journal_logic.test.js` locks the dedupe and orphan-prune helpers into `npm test`.
- Relaxed the live `microScout` no-DEX path so Tokyo can actually probe fresh launches before DexScreener indexes them. `scripts/maintain/momentum_sniper.remote.ts` now evaluates missing-pair bursts with a dedicated helper, uses lower raw flow floors plus a whale-flow exception, shortens the plain `no_dex_data_skip` cooldown, and treats weak apex dossiers as advisory for capped micro-scout probes unless the strategy profile explicitly requires the overlay. `config/strategy-profiles/active.strategy.json` now exposes those scout knobs directly, and `scripts/maintain/micro_scout_logic.test.js` locks the no-DEX decision math into the test suite.
- Restored the dead wallet-intelligence lane by adding a real `scripts/maintain/wallet_monitor.ts` producer for `signals/wallet_signals.json`. Tokyo had a monitor that only published raw `ALPHA_WALLET_UPDATE` balance snapshots, so the sniper and Gemma were reading a week-old stale alpha file. The new monitor persists wallet balance state, detects buy/sell deltas from tracked alpha and KOL wallets, writes executable `buy_signals` / `sell_signals`, appends wallet activity history for refinement, and uses `getParsedTokenAccountsByOwner` to cut the RPC fan-out that was contributing to Helius/Cloudflare 502 noise.
- Fixed a self-inflicted live-entry distortion in `scripts/maintain/momentum_sniper.remote.ts` where array-only `VELOCITY_SPIKE` events were being inflated into fake `25B/5S`, `20tx/min`, `2.0 SOL/60s` metrics. The sniper now hydrates those spikes from the real `signals/velocity.json` snapshot and falls back to that same file when the in-memory cache is stale, so Tokyo evaluates actual orderflow instead of synthetic conviction.
- Made the loss-streak cooldown a strategy-profile switch instead of a hidden code bypass, then enabled `liveTest.disableLossStreakPause` in `config/strategy-profiles/active.strategy.json` for the April 16 data-refine window so Tokyo keeps learning through short loss clusters without carrying a stale `pausedUntil` timer in state.
- Added two live false-positive guards in `scripts/maintain/momentum_sniper.remote.ts` after log review showed the normal lane still buying some already-large, flat names and then immediately tripping `APEX_EXIT thin_air_liquidity`. The sniper now rejects flow-only continuation when live 5-minute momentum is actually negative, and it runs a pre-entry thin-air liquidity check so million-dollar “sell according to apex before we even buy” structures get blocked upfront instead of paying a round-trip loss to discover the same fact.
- Fixed a live-entry blocker in `scripts/maintain/momentum_sniper.remote.ts` by preserving missing DexScreener 1-minute candles as unknown instead of coercing them to `0`, then letting strong live orderflow stand in as continuation only when that candle is absent. This keeps the normal and Last Stand lanes from self-rejecting on fake flat momentum while still refusing known weak 1-minute action.
- Added `scripts/maintain/continuation_signal_logic.ts` plus tests so the “missing 1m candle vs. real flat 1m candle” distinction is exercised outside the live loop, and widened the no-DEX micro-scout probe just enough to allow exceptional whale-flow bursts without reopening the general junk bucket.
- Filtered `PAPER_TRADE_...` rows out of `scripts/maintain/gemma4_auto_refiner.py` so Gemma stops learning from legacy ghost trades that were journaled before the live signature guards landed.
- Removed the sniper lane’s dependency on `src/utils/wsol_manager.ts` by switching `scripts/maintain/momentum_sniper.remote.ts`, `scripts/maintain/momentum_sniper.ts`, and `scripts/momentum_sniper.ts` to native-SOL balance sizing via `src/utils/native_sol_balance.ts`, then deleting the unused WSOL manager module entirely. The bot now sizes directly from spendable SOL while still using Jupiter’s native-SOL route mint for execution.
- Tightened the PRD-driven apex overlay so aggressive override behavior now requires real bot/follow-wallet confirmation and non-declining participation, added 6-hour volume into the conviction/exit path, and let Last Stand candidate ranking boost mints that already look manipulated in the GMGN smart-money data before the final holder check.
- Restored missing local bootstrap modules under `src/utils/` and `src/shared/` (`rpc_client`, `redis_bus`, `redis_config`, `trade_validator`) so the sniper workspace can load its shared dependencies locally instead of failing before strategy logic even starts.
- Added `scripts/maintain/native_sol_balance.test.js` and expanded `scripts/maintain/apex_predator_logic.test.js` so the native reserve math and stricter apex overlay behavior are covered by `npm test`.
- Added `scripts/maintain/apex_predator_logic.ts` as a pure conviction and exit-scoring helper so the PRD-style “apex predator” signals can be evaluated consistently across entry, replay, and future strategy tuning without burying more heuristics directly in the sniper loop.
- Wired GMGN smart-money and follow-wallet metadata into `scripts/maintain/momentum_sniper.remote.ts` as explicit conviction inputs, added a conservative micro-scout-only apex gate for no-DEX probes, and layered structural `APEX_EXIT` signals into the live exit loop for thin-liquidity million-dollar runs and smart-money exodus events.
- Added `scripts/maintain/apex_predator_logic.test.js`, a minimal `tsconfig.json`, and a real `npm test` script so the new entry/exit scoring rules are covered by the built-in Node test runner instead of living only as hand-tuned live logic.
- Added continuation-aware Last Stand filters in `scripts/maintain/momentum_sniper.remote.ts` so older or already-expanded candidates now need a live 1-minute continuation signal instead of being admitted on stale 5-minute momentum alone.
- Added a blowoff guard for Last Stand entries so names that already ran hard in 5 minutes but have flat short-term continuation get rejected before we buy into exhaustion.
- Extended the same continuation and blowoff rejection into the shared `trySnipe()` path so the normal velocity lane cannot bypass the anti-exhaustion filter just because treasury is above Last Stand mode.
- Increased open-position exit cadence from the coarse discovery loop to a 5-second live guard so configured stops and time exits land closer to intent during fast reversals.
- Tuned `scripts/active.strategy.json` with `minMomentum1mPct`, `blowoffMomentum5mPct`, and `requireContinuationAgeSeconds` to match the April 16 Tokyo live failure pattern: high 5-minute winners with weak 1-minute continuation were turning into immediate stop-outs.
- Added live ghost-signature guards so `PAPER_TRADE_...` swap results can no longer be journaled as real live BUY or SELL events in the sniper path.
- Added periodic open-position snapshots for unrealized SOL and percentage PnL so live monitoring can confirm current value, stop trigger, and exit intent while a trade is still open.
- Added the actual runtime profile path at `config/strategy-profiles/active.strategy.json` so local strategy edits now match the PM2 configuration the Tokyo VPS is using.
- Verified a clean live sample on Tokyo after stopping `pcp-sniper-paper`: `4YiLHDR4...` opened with a real BUY signature, emitted repeated unrealized-PnL position snapshots while open, and later closed with a real SELL plus realized PnL instead of regressing into `PAPER_TRADE_...` artifacts.
- Tightened the normal velocity lane so non-trending candidates use a shorter synthetic flow window instead of fake `buys60s * 60` conviction, and added explicit normal-mode rejects for synthetic-flow-only, flat-momentum, and negative-trend entries that were timing out near breakeven.
- Synced the exact patched sniper source onto Tokyo after a failed hand-edit attempt so the live PM2 entrypoint now matches the local project file byte-for-byte.
- Confirmed in fresh Tokyo logs that the synthetic velocity inflation is gone from new entries: corrected normal-lane candidates now print `150B/30S` instead of the old fake `1500B/300S` pattern.
- Verified the tighter acceptance funnel is active in live flow: fresh Tokyo samples now emit real `BLOWOFF REJECT` and `NEGATIVE TREND REJECT` events instead of only timing out or slipping deeper into the buy path.
- Moved the cheap momentum and liquidity gate ahead of the user-facing `Sniping ...` log so micro-size verification windows now reflect actual post-filter intent instead of names that were destined to be rejected a few steps later.
- Made the velocity-first loop respect existing per-mint cooldown keys before re-ranking candidates, so repeated `LIVE DUMP SKIP` and `NO DEX DATA SKIP` names stop burning scan budget every few seconds during flat market windows.
- Added the missing versioned `scripts/maintain/wiggum_loop.strategy_gate.json` and pointed it at `node scripts/strategy_gate.js` plus an engine-state validator, replacing the stale Tokyo-only GMGN holdings wrapper that was keeping Wiggum in false `retrying` state even while the real gate logic was healthy.
- 2026-04-21: Fixed the Wiggum yield loop so economically bad cycles no longer masquerade as healthy live-armed passes. `run_yield_cycle.js` now derives execution status, outcome code, and `economicAcceptance` from the real micro/alpha state, instead of hardcoding `live-armed` plus “Cycle completed successfully” whenever execution was enabled.
- 2026-04-21: Tightened the Wiggum validators and Hermes orchestration contract. `validate_yield_cycle.js` now rejects contradictory report states, `hermes_orchestrator.js` now distinguishes `success` vs `hold` vs `failure`, and `validate_orchestrator_output.js` only treats technical failures as orchestrator failures while preserving truthful economic holds.
- 2026-04-21: Restored local source-of-truth parity for Wiggum maintain paths by adding missing maintain wrappers/configs for `run_yield_cycle`, `validate_yield_cycle`, `wiggum_loop`, `preflight_loop`, `wiggum_loop.preflight.json`, and `wiggum_loop.yield_cycle.json`, so Tokyo no longer depends on stale remote-only Wiggum files.
- Added 24-hour trade-learning telemetry to `scripts/maintain/momentum_sniper.remote.ts` so every real BUY now captures token age, liquidity, micro-cap/FDV, momentum, holder concentration, and security buckets, while every SELL rolls those entry profiles into `signals/trade_profile_stats.json` for live bucket-level win-rate and PnL analysis.
- Added `scripts/maintain/trade_profile_summary.remote.js` as a lightweight Tokyo-side summary script so hourly or post-run reviews can rank the strongest and weakest age, liquidity, market-cap, and momentum buckets without hand-parsing `trade_journal.jsonl`.
- Fixed partial take-profit handling in `scripts/maintain/momentum_sniper.remote.ts` so scale-out sells keep the remainder position open with adjusted size and cost basis instead of orphaning leftover tokens while falsely journaling a full exit.
- Hardened `scripts/wiggum_loop.js` so the optional reviewer cleanly skips when no local Ollama server is listening on `127.0.0.1:11434`, and so configured HTTPS review endpoints use the correct transport instead of forcing plain HTTP.
- Restored the Gemma refinement lane by making `scripts/maintain/gemma4_auto_refiner.py` resolve the live project root dynamically, default to `gemma4:e4b`, and write recommendations into the active Tokyo `signals/` directory instead of an old migrated mount path.
- Added a managed `pcp-gemma4-refiner` PM2 entry and updated `scripts/maintain/gemma4_agent.ts` so both direct Gemma tests and the long-running refiner use the configured `OLLAMA_URL` endpoint consistently.
- Added the missing `scripts/maintain` Hermes orchestration files and the `wiggum-hermes-orchestrator` PM2 entry so Tokyo’s yield-orchestration layer is part of the live swarm again instead of existing only as stale `.swarm/hermes` state from April 15.
- Documented that `waiting restart` is expected for cron-driven Wiggum jobs between scheduled runs, while a totally missing PM2 app like Hermes indicates a real wiring gap rather than a runtime failure.
- Fixed the live sniper’s missed-target telemetry by removing a duplicate no-op `logMissedTarget` override, persisting structured reject events again, and adding `signals/missed_target_stats.json` so future refinement can see dominant rejection stages and reasons instead of inferring them from PM2 logs alone.
- Added `scripts/maintain/missed_target_outcome_summary.remote.py` to replay recent reject telemetry against GMGN 1-minute candles, so we can rank which rejection reasons were protective versus which ones hid recoverable continuation setups.
- Tuned the normal entry funnel with two narrow evidence-based overrides in `scripts/maintain/momentum_sniper.remote.ts`: a negative-trend recovery exception for strong short-horizon reversals and a thin-liquidity continuation exception for high-flow names just below the standard liquidity floor.
- Kept the broader safety gates intact after replay analysis: `normal_momentum_below_threshold`, `low_volume_skip`, `overbought_skip`, and holder-concentration style risk rejects were still filtering mostly poor follow-through or high-drawdown names, while `no_dex_data_skip` looked too toxic to relax globally.
- Added a dedicated `microScout` lane to the live runtime profile and the velocity-first sniper path so exceptional `no_dex_data_skip` bursts can be probed with fixed ultra-micro size instead of reopening the full normal lane on missing-liquidity names.
- Wired `scripts/maintain/momentum_sniper.remote.ts` to size `micro-scout` entries independently, tag them distinctly in learning/journal output, and use tight fixed exits (`12%` TP cap, `8%` SL, `3m` max hold, no partial TP) so we can learn from raw-velocity launches without letting them dominate live risk.
- 2026-04-18: Added adaptive micro-probe reserve clamping so the Tokyo refine lane preserves one 0.001 SOL scout plus fee buffer when balance falls below the configured reserve, instead of silently hard-blocking all new entries.
- 2026-04-18: Added a rolling terrain-memory layer for hot mints. The live sniper now keeps short per-mint dossiers of route quality, liquidity response, and price response, holds the noisiest route-live zero-liquidity micro probes for one extra confirmation sample, and blocks repeated strong-flow names that still show flat terrain after multiple observations.
- Fixed the actual Tokyo target drought: `pcp-velocity-stream` was trapped in a websocket `429` reconnect loop, leaving `signals/velocity.json` empty. The stream now uses an explicit rate-limit-aware WS subscription path with exponential backoff, idle-health reconnects, and PM2 deploy wiring so fresh movers can repopulate even when the WS provider pushes back.
- Added a non-Helius composite fallback for `pcp-velocity-stream`. When the live websocket feed is empty or unhealthy, the service now synthesizes `signals/velocity.json` from GMGN/Bags trending data plus executable profitable-wallet signals, so the sniper can keep assessing fresh targets without leaning on the saturated Helius stream.
- 2026-04-18: Stopped synthetic launchpad fallback pollution from dominating the velocity lane by ignoring initialize-only stream logs, marking composite fallback entries as synthetic, filtering weak `onchain-launchpad` fallback rows, and cooling down leftover placeholder `0.5 / 250 SOL` synthetic candidates in the sniper before they burn scan budget.
- 2026-04-18: Stopped full velocity snapshots from masquerading as incremental `VELOCITY SPIKE` events by keeping snapshot refreshes file-backed and classifying pubsub payloads as `delta` vs `snapshot` before the sniper triggers a hot scan.
- 2026-04-21: Reopened the live route-driven entry lane by adding a short flat-GMGN missing-1m recheck, letting strong route-live micro-only names bypass the blanket <$1K/1h skip, and lowering the qualifier threshold only for route-live terrain-confirmed micro entries so fresh low-liquidity candidates can reach `FINAL GATE READY` instead of dying on stale hourly confidence.
- 2026-04-21 - Re-enabled staged profit-taking for micro-scout and route-live micro entries, added catch-up TP stages so vertical moves can bank gains in a single loop pass, and aligned live trailing exits with the same dynamic floor the status line advertises.
- 2026-04-21 - Pruned plateau-style `gmgn-bridge` names out of the composite velocity fallback feed when they are flat on `5m`, flat on `1h`, and still lack fresh smart-money or turnover support, while preserving bags signals and executable-wallet confirmations so scan budget stays focused on names with real movement.
- 2026-04-21 - Reopened one more narrow route-live entry path by letting missing-`1m` continuation pass when a live Jupiter route is present, raw flow is explosive, and terrain is still near peak, so strong zero-liquidity bursts do not die on the legacy `72%` continuation floor alone.
- 2026-04-21 - Split the preflight overbought hard ceiling so route-live low-liquidity bursts can stay eligible up to a higher route-live-only ceiling and reach terrain confirmation, instead of being auto-killed by the generic overbought wall before route/flow context is considered.
